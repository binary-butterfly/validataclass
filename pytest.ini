[pytest]
addopts =
    -ra
    --import-mode=importlib
    --cov-context=test
    --cov-report=

testpaths = tests
python_files = *_test.py *Test.py
python_classes = *Test

# Fail on warnings
filterwarnings = error
